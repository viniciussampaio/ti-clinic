NODE_ENV=development
PORT=3000

# Database (docker-compose overrides DB_HOST/DB_PORT)
DB_DATABASE=clinic
DB_USERNAME=root
DB_PASSWORD=root

# Auth
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=1d

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:8080

