NODE_ENV=development
PORT=3000

# Docker Compose: porta publicada no host (API escuta em PORT dentro do container).
# API_PORT_EXPOSE=3001
# DB_PORT_EXPOSE=3307

DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=clinic
DB_USERNAME=root
DB_PASSWORD=root

JWT_SECRET=change-me
JWT_EXPIRES_IN=1d

CORS_ORIGIN=http://localhost:3000

